from setuptools import setup
#this line is used for testing of git & github
setup(name='floodsystem',
      version='0.1',
      description='CUED Part IA flood warning system exercise',
      packages=['floodsystem'],
      )
